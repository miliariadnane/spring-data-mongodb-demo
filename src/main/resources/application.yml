spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test
      username: rootuser
      password: rootpassword
      authentication-database: admin
      uri: mongodb://rootuser:rootpassword@localhost:27017/test?authSource=admin
  profiles:
    active: dev
